import { ProductDirective } from './product.directive';

describe('ProductDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductDirective();
    expect(directive).toBeTruthy();
  });
});
